import TeamPageClient from "@/components/team/TeamPageClient";

export const metadata = {
  title: "Team | Student Managed Investment Fund",
};

export default function TeamPage() {
  return <TeamPageClient />;
}
